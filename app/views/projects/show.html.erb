<% if notice != nil%>
  <p id ="notice" class = "alert alert-success"><%=notice%></p>
<%end%>
<div>
  <h2><%= @project.name %>

  <%= link_to 'Destroy', project_path(@project), method: :delete, :class=>'btn btn-danger pull-right' %>
  <%= link_to 'Edit', edit_project_path(@project), :class=>'btn btn-primary pull-right' %>
</h2>
</div>

  <!-- <%= link_to 'To Index', projects_path %> -->
